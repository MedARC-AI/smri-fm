[build-system]
requires = ["setuptools>=64", "setuptools_scm[toml]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "nanobrain"
dynamic = ["version"]
description = "MedARC nanobrain"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "braceexpand",
    "datasets",
    "einops",
    "huggingface-hub",
    "ipython",
    "jaxtyping",
    "jupyter",
    "matplotlib",
    "nibabel",
    "numpy",
    "omegaconf",
    "pandas",
    "pre-commit",
    "pytest",
    "pyyaml",
    "s3fs",
    "scikit-learn",
    "scipy",
    "seaborn",
    "tabulate",
    "templateflow",
    "timm",
    "torch==2.8.0",
    "torchvision",
    "tqdm",
    "wandb",
    "webdataset",
]

[project.optional-dependencies]
neurojepa = ["neurojepa"]

[tool.uv.sources]
neurojepa = { git = "https://github.com/clane9/Neuro-JEPA", rev = "e9dff69446f643b6cab8213c15a44523301e51da" }

[tool.setuptools_scm]
version_file = "src/nanobrain/_version.py"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = ["F722"]

[tool.pytest.ini_options]
norecursedirs = ["third_party", ".scratch"]
